class Output {
    constructor(options) {
        this.script = '';
        this.value = 0.0;

        if (options) {
            this.script = Script.fromAddress(script);
            this.value = value;
        }
    }
}